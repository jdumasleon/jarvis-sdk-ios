import Foundation

/// Log levels for crash reporting and monitoring
public enum LogLevel: String {
    case debug = "debug"
    case info = "info"
    case warning = "warning"
    case error = "error"
    case fatal = "fatal"
}
